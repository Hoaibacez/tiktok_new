const config = {
    mainWidth: 1100,
    routes: {
        home: '/',
        following: '/following',
        postDetail: '/:nickname/video/:videoId',
        login: '/login'
    }
}

export default config
